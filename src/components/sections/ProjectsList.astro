---

---

<style>
  .fade-in {
    opacity: 0;
    transform: translateY(20px);
    animation: fadeInUp 1s ease-out forwards;
  }

  @keyframes fadeInUp {
    from {
      opacity: 0;
      transform: translateY(20px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .project-card {
    background: rgba(255, 255, 255, 0.05);
    border: 1px solid rgba(255, 255, 255, 0.1);
    border-radius: 10px;
    overflow: hidden;
    transition: transform 0.3s ease-in-out, box-shadow 0.3s ease-in-out;
  }

  .project-card:hover {
    transform: translateY(-5px);
    box-shadow: 0 0 15px rgba(34, 197, 94, 0.5);
  }
</style>

<section class="bg-gray-900 text-white py-20">
  <div class="max-w-7xl mx-auto px-6 text-center fade-in">
    <h2 class="text-4xl font-bold text-green-400">Proyectos Destacados</h2>
    <p class="text-lg text-gray-300 mb-12">
      Aquí algunos de los proyectos en los que he trabajado recientemente.
    </p>

    <!-- Contenedor de Proyectos -->
    <div id="projects-container" class="grid grid-cols-1 md:grid-cols-3 gap-8"></div>
  </div>
</section>


